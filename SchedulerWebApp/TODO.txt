~~DONE~~ Add 'Search' link along the top of Main Controller layout

~~DONE~~ MainController.cs
    - redirect ReportSearch() to ReportResult(), sending result of CustomerProcessor.CreateReport(start, end)
    - create a View for ReportResult(), similar to ViewAppointments() 

/Home/Verify
    - Update text here
    - Add a redirect back to Index?

/Home/About and /Home/Contact
    - Either update or delete

Main/Index/Home needs to be changed into something

~~DONE~~ /Main/ViewCustomers complete

/Main/EditCustomer
    - Viewbag -> EditCustomer and CustomerModel visible

/Main/DeleteCustomer
    - customerId is visible

~~DONE~~ /Main/CreateCustomer

/Main/ViewAppointments
    - Maybe set default to sort by Appointment Start?

~~DONE~~ /Main/DeleteAppointment

/Main/EditAppointment
    - Viewbag EditAppointment and AppointmentModel visible
    - Change "CustomerID" to "Customer Name"
    - change "Appointment Start" to _something_
    - Razor view -> semicolon visible after dropdownlist
    - *Back to List* link directs to index -> change it to ViewAppointments

/Main/CreateAppointment
    - No viewbag at all, add something
    - Change "CustomerID" to "Customer Name"
    - change "Appointment Start" to _something_
    - Razor view -> semicolon visible after dropdownlist
    - *Back to List* link directs to index -> change it to ViewAppointments

/Main/ReportSearch
    - Viewbag ReportSearch and ReportModel visible
    - *Back to List* link directs to index -> This might be okay, but change to "Back to Index/Home"?
    - ***Finish JavaScript for pickers!
        - Set time picker to off
        - set beginning default time to 00:00
        - Set end default to 23:59

/Main/ReportResult ??
    - Currently shows as ReportSearch, possibly correct?
    - Viewbag shows ReportResult
    - **Remove "Create New" link at top of page
    - Add *Back to Index* link?

